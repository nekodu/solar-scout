# Secrets — NEVER commit
.env
.oci/
*.pem

# Runtime data / state
market.db
private/
out*/
.vm-ready
*.log

# Python
.venv/
__pycache__/
*.pyc

# OS / editor
.DS_Store
*.swp

# Claude Code — local project notes, never commit
CLAUDE.md
.claude/

# personal infra automation (contains real OCIDs / account identifiers)
scripts/oci-acquire.sh
scripts/setup-vm.sh
scripts/vm-warm-deploy.sh
scripts/oci-hunt-loop.sh
scripts/oci-supervisor.sh
visits.jsonl
.admin_key
.ntfy_topic
.oci-hunt.lock
STATUS.md
